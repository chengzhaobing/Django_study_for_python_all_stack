##

```html

  <div class="col-sm-10">
       <input type="text" name="title" class="form-control" placeholder="部 门">
  </div>

```
```
   depart_info = request.POST.get("input输入框name参数")
    其中input输入框的name为接受参数的 接收对象
```

> depart_info = request.POST.get("title") 必须与这里的title一致
>
#这里是最重要的 修复了一辈子

# 这里的tile接受的是输入框的名称必须设置输入框名称